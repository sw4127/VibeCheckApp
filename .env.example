# Copy this file to `.env.local` and fill in real values. Never commit `.env.local`.

# Anthropic API key — used server-side only for narration. Paste your own key.
ANTHROPIC_API_KEY=

# Pinned model snapshots (override defaults if needed).
# Stage 1 narration runs on the cheaper model; premium report (Stage 2) uses the stronger one.
ANTHROPIC_MODEL_NARRATION=claude-haiku-4-5-20251001

# Public base URL used on the share card / OG image (no trailing slash).
# Local dev default; set to the deployed URL in production.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
